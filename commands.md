# Useful commands

## Aliases
alias ll='ls -la'

alias update='sudo pacman -Syu'

alias cls='clear'

## Ip Address
ip addr show

## User list
cut -d: -f1 /etc/passwd

## Sudoers
sudo cat /etc/sudoers
